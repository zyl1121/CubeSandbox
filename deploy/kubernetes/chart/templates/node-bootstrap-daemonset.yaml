{{- if .Values.cubeNode.enabled }}
# Node prep DaemonSet (wait-pvm-host + node-init + write node-prep-ready).
# PVM host install is on cube-node-pvm. Low-churn: bumping nodeInit recreates
# THIS pod only, not the Big Pod. Artifact install stays on cube-node-installer.
apiVersion: {{ include "cube.nodeDaemonSetAPIVersion" . }}
kind: DaemonSet
metadata:
  name: {{ include "cube.nodeBootstrapName" . }}
  labels:
    {{- include "cube.labels" . | nindent 4 }}
    app.kubernetes.io/component: cube-node-bootstrap
spec:
  updateStrategy:
    type: {{ .Values.cubeNodeBootstrap.updateStrategy.type | default "RollingUpdate" }}
    {{- if .Values.cubeNodeBootstrap.updateStrategy.rollingUpdate }}
    rollingUpdate:
      maxUnavailable: {{ .Values.cubeNodeBootstrap.updateStrategy.rollingUpdate.maxUnavailable | default 1 }}
      {{- with .Values.cubeNodeBootstrap.updateStrategy.rollingUpdate.maxSurge }}
      maxSurge: {{ . }}
      {{- end }}
    {{- end }}
  selector:
    matchLabels:
      {{- include "cube.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: cube-node-bootstrap
  template:
    metadata:
      labels:
        {{- include "cube.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: cube-node-bootstrap
      annotations:
        {{- with .Values.cubeNodeBootstrap.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "cube.nodeServiceAccountName" . }}
      automountServiceAccountToken: true
      # nsenter --target 1 for node-init (same privilege model as former Big Pod inits).
      hostPID: true
      dnsPolicy: ClusterFirst
      terminationGracePeriodSeconds: {{ .Values.cubeNodeBootstrap.terminationGracePeriodSeconds }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- include "cube.computePlacement" . | nindent 6 }}
      initContainers:
        - name: invalidate-stale-node-prep
          image: {{ include "cube.cubeImage" (dict "image" .Values.images.nodeInit "context" $) | quote }}
          imagePullPolicy: {{ .Values.images.nodeInit.pullPolicy }}
          command: ["/bin/sh", "-ec"]
          args:
            - |
              . /scripts/node-prep-lib.sh
              ready="$(node_prep_ready_path)"
              [ -f "$ready" ] || exit 0
              old_generation="$(sed -n 's/^prep_generation=//p' "$ready")"
              old_node_init="$(sed -n 's/^node_init_enabled=//p' "$ready")"
              if [ "$old_generation" != "$PREP_GENERATION" ] || [ "$old_node_init" != "$NODE_INIT_ENABLED" ]; then
                invalidate_node_prep_ready
              fi
          env:
            - name: STATE_DIR
              value: {{ .Values.hostPaths.bootstrapState | quote }}
            - name: PREP_GENERATION
              value: {{ .Values.cubeNodeBootstrap.prepGeneration | quote }}
            - name: NODE_INIT_ENABLED
              value: {{ ternary "1" "0" .Values.bootstrap.nodeInit.enabled | quote }}
          volumeMounts:
            - name: bootstrap-state
              mountPath: {{ .Values.hostPaths.bootstrapState }}
        - name: wait-pvm-host
          image: {{ include "cube.cubeImage" (dict "image" .Values.images.nodeInit "context" $) | quote }}
          imagePullPolicy: {{ .Values.images.nodeInit.pullPolicy }}
          command: ["/usr/bin/tini", "-s", "--", "/scripts/wait-pvm-host.sh"]
          securityContext:
            privileged: {{ .Values.security.privileged }}
          env:
            {{- include "cube.timezoneEnv" . | nindent 12 }}
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: HOST_ROOT
              value: /host
            - name: STATE_DIR
              value: {{ .Values.hostPaths.bootstrapState | quote }}
            - name: PVM_FEATURE_ENABLED
              value: {{ ternary "1" "0" .Values.bootstrap.pvmHostKernel.enabled | quote }}
            - name: CUBE_PVM_ENABLE
              value: {{ ternary "1" "0" .Values.cubeNode.pvmGuestKernel.enabled | quote }}
            - name: ALLOW_PVM_LABEL
              value: cube.tencent.com/allow-pvm-bootstrap
            - name: DESIRED_KERNEL_PATTERN
              value: {{ .Values.bootstrap.pvmHostKernel.desiredKernelPattern | quote }}
            - name: KERNEL_BOOT_ARGS
              value: {{ .Values.bootstrap.pvmHostKernel.bootArgs | quote }}
            - name: WAIT_TIMEOUT_SECONDS
              value: {{ .Values.cubeNodeBootstrap.pvmWaitTimeoutSeconds | default "900" | quote }}
          volumeMounts:
            {{- include "cube.bootstrapHostVolumeMounts" . | nindent 12 }}
        {{- if .Values.bootstrap.nodeInit.enabled }}
        - name: cube-node-init
          image: {{ include "cube.cubeImage" (dict "image" .Values.images.nodeInit "context" $) | quote }}
          imagePullPolicy: {{ .Values.images.nodeInit.pullPolicy }}
          securityContext:
            privileged: {{ .Values.security.privileged }}
          env:
            {{- include "cube.timezoneEnv" . | nindent 12 }}
            - name: HOST_ROOT
              value: /host
            - name: STATE_DIR
              value: {{ .Values.hostPaths.bootstrapState | quote }}
            - name: NODE_INIT_ENABLED
              value: "1"
            - name: PVM_ENABLED
              value: {{ ternary "1" "0" .Values.bootstrap.pvmHostKernel.enabled | quote }}
            - name: SKIP_IF_NODE_PREP_READY
              value: "true"
            - name: DATA_CUBELET
              value: {{ .Values.hostPaths.dataCubelet | quote }}
            - name: REQUIRE_KVM
              value: {{ .Values.bootstrap.nodeInit.requireKVM | quote }}
            - name: REQUIRE_XFS
              value: {{ .Values.bootstrap.nodeInit.requireXFS | quote }}
            - name: CHMOD_KVM
              value: {{ .Values.bootstrap.nodeInit.chmodKVM | quote }}
            - name: LOAD_KVM_MODULE
              value: {{ .Values.bootstrap.nodeInit.loadKVMModule | quote }}
            - name: WRITE_UDEV_RULE
              value: {{ .Values.bootstrap.nodeInit.writeUdevRule | quote }}
            - name: CREATE_HOST_DIRS
              value: {{ .Values.bootstrap.nodeInit.createHostDirs | quote }}
            - name: CHECK_MASTER_CONNECTIVITY
              value: {{ .Values.bootstrap.nodeInit.checkMasterConnectivity | quote }}
            - name: CUBE_MASTER_ENDPOINT
              value: {{ include "cube.masterEndpoint" . | quote }}
            - name: LOOPBACK_ENABLED
              value: {{ .Values.bootstrap.nodeInit.dataCubelet.loopback.enabled | quote }}
            - name: LOOPBACK_IMAGE_PATH
              value: {{ .Values.bootstrap.nodeInit.dataCubelet.loopback.imagePath | quote }}
            - name: LOOPBACK_SIZE
              value: {{ .Values.bootstrap.nodeInit.dataCubelet.loopback.size | quote }}
            - name: CHECK_MEMORY
              value: {{ .Values.bootstrap.nodeInit.checkMemory | quote }}
            - name: MIN_MEMORY_KB
              value: {{ .Values.bootstrap.nodeInit.minMemoryKB | quote }}
            - name: CHECK_CGROUP_CPU
              value: {{ .Values.bootstrap.nodeInit.checkCgroupCPU | quote }}
            - name: CHECK_BPF_FS
              value: {{ .Values.bootstrap.nodeInit.checkBPFFS | quote }}
            - name: CHECK_GLIBC
              value: {{ .Values.bootstrap.nodeInit.checkGlibc | quote }}
            - name: CHECK_CIDR
              value: {{ .Values.bootstrap.nodeInit.checkCIDR | quote }}
            - name: CHECK_CUBECOW_DEPS
              value: {{ .Values.bootstrap.nodeInit.checkCubecowDeps | quote }}
            - name: CUBE_PVM_ENABLE
              value: {{ ternary "1" "0" .Values.cubeNode.pvmGuestKernel.enabled | quote }}
            - name: CUBE_SANDBOX_NETWORK_CIDR
              value: {{ .Values.cubeNode.network.cidr | quote }}
            - name: CUBE_SANDBOX_NETWORK_CIDR_SKIP_CONFLICT_CHECK
              value: {{ ternary "1" "0" .Values.cubeNode.network.cidrSkipConflictCheck | quote }}
            - name: PREP_GENERATION
              value: {{ .Values.cubeNodeBootstrap.prepGeneration | quote }}
            - name: DESIRED_KERNEL_PATTERN
              value: {{ .Values.bootstrap.pvmHostKernel.desiredKernelPattern | quote }}
            - name: KERNEL_BOOT_ARGS
              value: {{ .Values.bootstrap.pvmHostKernel.bootArgs | quote }}
          volumeMounts:
            {{- include "cube.bootstrapHostVolumeMounts" . | nindent 12 }}
            {{- include "cube.bootstrapDataVolumeMounts" . | nindent 12 }}
        {{- end }}
      containers:
        - name: write-node-prep-ready
          image: {{ include "cube.cubeImage" (dict "image" .Values.images.waitNodePrep "context" $) | quote }}
          imagePullPolicy: {{ .Values.images.waitNodePrep.pullPolicy }}
          command: ["/usr/bin/tini", "-s", "--", "/scripts/write-node-prep-ready.sh"]
          securityContext:
            seccompProfile:
              type: RuntimeDefault
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
          env:
            {{- include "cube.timezoneEnv" . | nindent 12 }}
            - name: STATE_DIR
              value: {{ .Values.hostPaths.bootstrapState | quote }}
            - name: PREP_GENERATION
              value: {{ .Values.cubeNodeBootstrap.prepGeneration | quote }}
            - name: PVM_ENABLED
              value: {{ ternary "1" "0" .Values.bootstrap.pvmHostKernel.enabled | quote }}
            - name: NODE_INIT_ENABLED
              value: {{ ternary "1" "0" .Values.bootstrap.nodeInit.enabled | quote }}
            - name: DESIRED_KERNEL_PATTERN
              value: {{ .Values.bootstrap.pvmHostKernel.desiredKernelPattern | quote }}
            - name: KERNEL_BOOT_ARGS
              value: {{ .Values.bootstrap.pvmHostKernel.bootArgs | quote }}
            {{- if and (not .Values.bootstrap.pvmHostKernel.enabled) (not .Values.bootstrap.nodeInit.enabled) }}
            - name: PREP_MODE
              value: noop
            {{- end }}
          volumeMounts:
            - name: bootstrap-state
              mountPath: {{ .Values.hostPaths.bootstrapState }}
          {{- with .Values.cubeNodeBootstrap.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        {{- include "cube.bootstrapVolumes" . | nindent 8 }}
{{- end }}
