# SPDX-License-Identifier: Apache-2.0
# Copyright (C) 2026 Tencent. All rights reserved.

worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include mime.types;
    default_type application/octet-stream;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;

    gzip on;
    gzip_types text/plain text/css application/javascript application/json application/xml;

    # Only send "Connection: upgrade" upstream for real WebSocket handshakes;
    # plain requests get an empty value so they are not mistaken for upgrades.
    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      '';
    }

    server {
        listen 80;
        server_name _;

        root /usr/share/nginx/html;
        index index.html;

        location = /cubeapi {
            return 308 /cubeapi/;
        }

        # Same-origin forward of sandbox traffic (incl. the OpenClaw gateway UI/wss)
        # to cube-proxy. Serving OpenClaw under the dashboard origin keeps its
        # localStorage in the same bucket the dashboard can clear, which is the
        # demo-time mitigation for OpenClaw WebSocket crosstalk. cube-proxy routes
        # by the /sandbox/<id>/<port>/ path, so the Host header is irrelevant here.
        location ^~ /sandbox/ {
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_read_timeout 7206s;
            proxy_send_timeout 7206s;

            proxy_pass __SANDBOX_PROXY_UPSTREAM__;
        }

        # CubeOps (ops/admin endpoints) — rewrite /opsapi → /api
        location /opsapi/ {
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;

            proxy_pass __CUBE_OPS_UPSTREAM__/api/;
        }

        # CubeAPI SDK endpoints → CubeOps (direct CubeMaster, JWT auth)
        # Frontend bundle uses /cubeapi/v1/* as SDK_BASE; route to CubeOps
        # /api/v1/sdk/* which calls CubeMaster directly.
        location /cubeapi/v1/ {
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;

            rewrite ^/cubeapi/v1/(.*)$ /api/v1/sdk/$1 break;
            proxy_pass __CUBE_OPS_UPSTREAM__;
        }

        # Health check (no auth needed) — served by CubeOps :3010.
        # CubeOps exposes GET /health without JWT; WebUI does not depend on
        # CubeAPI for this, so the /health upstream points to CubeOps.
        location = /health {
            proxy_pass __WEB_UI_UPSTREAM__;
        }

        # SDK endpoints → CubeOps (JWT auth + direct CubeMaster)
        # Browser navigation (no Authorization header) loads the SPA.
        # API calls (has Authorization: Bearer <jwt>) go to CubeOps.
        location ~ ^/(sandboxes|v2/sandboxes|templates|snapshots) {
            if ($http_authorization = "") {
                return 418;
            }
            add_header Vary "Authorization" always;
            add_header Cache-Control "no-store" always;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;

            rewrite ^/(.*)$ /api/v1/sdk/$1 break;
            proxy_pass __CUBE_OPS_UPSTREAM__;
        }

        # SPA fallback for browser navigation to SDK paths.
        error_page 418 = @spa_fallback;
        location @spa_fallback {
            root /usr/share/nginx/html;
            try_files /index.html =404;
            add_header Vary "Authorization" always;
            add_header Cache-Control "no-store, no-cache, must-revalidate" always;
        }

        location /assets/ {
            try_files $uri =404;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
